interface:
  display_name: "Local Claude Code Bypass Permissions"
  short_description: "Install a global PreToolUse hook that bypasses Claude Code permission prompts on macOS or Linux, with ssh/scp/sftp/rsync rewrite support."
  default_prompt: "Run /local-mac-claude-bypass-permissions to install the permission bypass hook and wrappers."
